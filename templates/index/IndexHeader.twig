{# templates/contact.html.twig #}

<div class="row justify-content-center {% if darkbg %} bg-dark rounded-5 border border-light {% endif %}">
	<a class="col-auto pt-1" href="#{{ textRef }}">
		<h4 class="text-center text-capitalize mb-4 white-text">{{ headText }}</h4>
	</a>
</div>

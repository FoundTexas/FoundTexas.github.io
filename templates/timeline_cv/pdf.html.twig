<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<title>CV</title>
		<style>
			body {
				font-family: Arial, sans-serif;
				font-size: 13px;
			}

			.row {
				text-align: center;
				padding: 0.5rem !important;
			}
			.milestone {
				margin-bottom: 2px;
			}
			.project,
			.tag,
			.bullet {
				margin-left: 2px;
			}
			.clearfix::after {
				content: "";
				clear: both;
				display: table;
			}
			.left {
				float: left;
			}
			.right {
				float: right;
			}
			.center {
				background-color: #f0f0f0;
				text-align: center;
			}
			.py-2 {
				padding-top: 0.5rem !important;
				padding-bottom: 0.5rem !important;
			}
		</style>
	</head>
	<body>
		<h4>Rodolfo León Gasca - Computer Science Engineer</h4>

		<p>rolega01@gmail.com |  55 4913 8324 | GitHub | LinkedIn |
			<a href="https://foundtexas.mx">Portfolio</a>
		</p>

		<p>I'm Rodolfo, computer scientist specialized in 3D Graphics, with skill on Project Management, Web and Game Development. I`m a team player and believe that diverse perspectives fuel innovation, leading to significant and immersive experiences for the users.</p>

		<div class="row py-2">
			<strong>Experience</strong>
		</div>

		{% for id, milestone in timeline_events %}
			<div class="milestone clearfix">
				<div class="clearfix">
					<strong class="left">{{ milestone.organization }}</strong>
					<p class="right">{{ milestone.location }}</p>
				</div>

				<div class="clearfix">
					<p class="left">{{ milestone.name }}</p>

					<div class="right">
						<p>{{ milestone.startDate|date('Y-m-d') }}
							-
							{{ milestone.endDate ? milestone.endDate|date('Y-m-d') : 'Current'}}</p>
					</div>
				</div>

				<ul class="bullets">
					{% for bullet in milestone.bullets %}
						<li class="bullet">
							<p>{{ bullet }}</p>
						</li>
					{% endfor %}
				</ul>

				<div class="tags">
					<h3>Tags:</h3>
					{% for tag in milestone.tags %}
						<div class="tag">
							<p>{{ tag }}</p>
						</div>
					{% endfor %}
				</div>
			</div>
		{% endfor %}
	</body>
</html>
